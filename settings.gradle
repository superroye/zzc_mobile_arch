include ':librarys', ':h5cache',
        ':platformCore',
        ':platformSdk',
        ':service-compiler',
        ':service-annotation',
        ':commonBiz',
        ':app',
        ':base'

file('librarys').eachDir { dir ->
    include dir.name
    project(":${dir.name}").projectDir = dir
}